//
// Created by Jake on 4/22/2017.
//

#include "ResultSet.h"
/*
ResultSet::ResultSet(AnalyzerType at)
{
    if (at == DenialOfService)
    {
        m_Results.add("Likely Attackers", findLikelyAttackers());
        m_Results.add("Possible Attackers", findPossibleAttackers());
        m_Results.add("Attack Period", findAttackPeriod());
        m_Results.add("Timeframe", findTimeframe());
    }
    else if (at == PortScan)
    {
        m_Results.add("Likely Attackers", findLikelyAttackers());
        m_Results.add("Possible Attackers", findPossibleAttackers());
        m_Results.add("Port Count", findPortCount());
    }
    else
    {
        throw std::string("This is an unsupported Analyzer.");
    }
}
*/
void ResultSet::print(std::ostream& out)
{

}